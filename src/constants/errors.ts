const possibleErrors = Object.freeze({
  'NO_PERMISSION': 'The site does not have the required permissions for this action',
  'USER_CANCEL_POPUP': 'User cancelled the login'
});

export default possibleErrors;
